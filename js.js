// let spn = document.querySelectorAll("span");
// let btn = document.querySelectorAll("button");

// btn.forEach((item) => {
//   item.addEventListener("mousedown", () => {
//     spn.forEach((elem) => elem.classList.add(".active"));
//   });
// });
